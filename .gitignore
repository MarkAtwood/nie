
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Rust build artifacts
target/

# Python
__pycache__/
*.pyc

# Archives
*.zip

# Identity key (never commit secrets)
identity.key

# Agent instruction files (local tooling only, not for contributors)
AGENTS.md

# Coordinator audit logs and agent worktrees
audit/
.claude/worktrees/
.codemagic.env
lib/src/rust/
